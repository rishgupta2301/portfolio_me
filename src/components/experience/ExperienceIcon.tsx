import React from 'react';
import { Briefcase } from 'lucide-react';

const ExperienceIcon = () => {
  return <Briefcase className="text-purple-500 dark:text-purple-400" size={20} />;
};

export default ExperienceIcon;